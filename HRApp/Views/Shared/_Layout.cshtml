<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>HRMS</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" />
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">HRMS</a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "Home")">Home</a></li>
                    <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "Company")">Companies</a></li>
                    <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "Designation")">Designations</a></li>
                    <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "Department")">Departments</a></li>
                    <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "Shift")">Shifts</a></li>
                    <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "Employee")">Employees</a></li>
                    <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "Attendance")">Attendance</a></li>
                </ul>
                <div class="d-flex">
                    <select id="companyFilter" class="form-select me-2" onchange="filterByCompany()">
                        <option value="">All Companies</option>
                        @foreach (var company in ViewBag.Companies ?? new List<dynamic>())
                        {
                            <option value="@company.Id" selected="@(ViewBag.SelectedCompanyId == company.Id)">@company.ComName</option>
                        }
                    </select>
                </div>
            </div>
        </div>
    </nav>
    <div class="container">
        @RenderBody()
    </div>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
    <script>
        function filterByCompany() {
            var selectedId = $("#companyFilter").val();
            if (selectedId) {
                $.ajax({
                    url: '@Url.Action("SetSelectedCompany", "Home")',
                    type: "POST",
                    data: { companyId: selectedId },
                    success: function () {
                        window.location.href = '@Url.Action("Index", "Company")';  
                    }
                });
            } else {
                window.location.href = '@Url.Action("Index", "Company")'; 
            }
        }
    </script>
</body>
</html>
@* <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - HRApp</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">HRMS</a>
            <div class="navbar-nav">
                <a class="nav-link" href="@Url.Action("Index", "Home")">Home</a>
                <a class="nav-link" href="@Url.Action("Index", "Company")">Companies</a>
                <a class="nav-link" href="@Url.Action("Index", "Designation")">Designations</a>
                <a class="nav-link" href="@Url.Action("Index", "Department")">Departments</a>
                <a class="nav-link" href="@Url.Action("Index", "Shift")">Shifts</a>
                <a class="nav-link" href="@Url.Action("Index", "Employee")">Employees</a>
                <a class="nav-link" href="@Url.Action("Index", "Attendance")">Attendance</a>
                <a class="nav-link" href="@Url.Action("Index", "AttendanceSummary")">Attendance Summary</a>
                <a class="nav-link" href="@Url.Action("Index", "Salary")">Salary</a>
            </div>
        </div>
    </nav>
    <div class="container mt-4">
        @RenderBody()
    </div>

   
    @await RenderSectionAsync("Scripts", required: false)

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> *@