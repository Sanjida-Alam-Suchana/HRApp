@model IEnumerable<HRApp.Models.Salary>

@{
    ViewData["Title"] = "Salary List";
    var selectedComId = ViewBag.SelectedComId as Guid?;
    var selectedYear = ViewBag.SelectedYear as int? ?? DateTime.Now.Year;
    var selectedMonth = ViewBag.SelectedMonth as int? ?? DateTime.Now.Month;
}

<h1>Salary List</h1>

<!-- Form for Filter (AJAX) and Generate (AJAX POST) -->
<form id="salaryForm">
    @Html.AntiForgeryToken()
    <div class="form-row">
        <div class="form-group col-md-4">
            <label>Select Company <span class="text-danger">*</span></label>
            <select name="comId" id="companyDropdown" class="form-control" required>
                <option value="">All Companies</option>
                @foreach (var company in ViewBag.Companies)
                {
                    <option value="@company.ComId" selected="@(selectedComId.HasValue && company.ComId == selectedComId.Value)">@company.ComName</option>
                }
            </select>
        </div>
        <div class="form-group col-md-3">
            <label>Select Year <span class="text-danger">*</span></label>
            <select name="year" id="yearDropdown" class="form-control" required>
                @for (int y = DateTime.Now.Year - 5; y <= DateTime.Now.Year + 5; y++)
                {
                    <option value="@y" selected="@(y == selectedYear)">@y</option>
                }
            </select>
        </div>
        <div class="form-group col-md-3">
            <label>Select Month <span class="text-danger">*</span></label>
            <select name="month" id="monthDropdown" class="form-control" required>
                @for (int m = 1; m <= 12; m++)
                {
                    <option value="@m" selected="@(m == selectedMonth)">@(m.ToString("D2"))</option>
                }
            </select>
        </div>
        <div class="form-group col-md-2">
            <br />
            <button type="button" id="filterBtn" class="btn btn-primary btn-block">Filter</button>
        </div>
    </div>
    <div class="form-row">
        <div class="col-md-12">
            <button type="button" id="generateBtn" class="btn btn-success">Generate Salary</button>
            <span id="generateStatus" class="ml-3" style="display:none;">Ready</span>
        </div>
    </div>
</form>

<hr />

<!-- Salary List Table -->
<div class="table-responsive">
    <table class="table table-striped" id="salaryTable">
        <thead class="thead-dark">
            <tr>
                <th>Employee Name</th>
                <th>Year-Month</th>
                <th>Gross</th>
                <th>Basic</th>
                <th>HRent</th>
                <th>Medical</th>
                <th>Absent Days</th>
                <th>Absent Amount</th>
                <th>Payable Amount</th>
                <th>Paid Amount</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody id="salaryTableBody">
            @if (!Model.Any())
            {
                <tr>
                    <td colspan="12" class="text-center text-muted py-4">
                        No salaries found. Generate some or adjust filters.
                    </td>
                </tr>
            }
            else
            {
                @foreach (var item in Model)
                {
                    <tr id="row-@item.SalaryId" data-comid="@item.ComId">
                        <td>@(item.Employee?.EmpName ?? "Not Found")</td>
                        <td>@item.SalaryMonth</td>
                        <td>@item.Gross</td>
                        <td>@item.Basic</td>
                        <td>@item.HRent</td>
                        <td>@item.Medical</td>
                        <td>@item.AbsentDays</td>
                        <td>@item.AbsentAmount</td>
                        <td>@item.PayableAmount</td>
                        <td>@item.PaidAmount</td>
                        <td>@(item.IsPaid ? "Paid" : "Unpaid")</td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.SalaryId">Edit</a> |
                            <button class="payBtn" data-id="@item.SalaryId">Pay</button> |
                            <button class="deleteBtn" data-id="@item.SalaryId">Delete</button>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function () {
            console.log('Script loaded - jQuery ready');

            // Get the anti-forgery token
            var token = $('input[name="__RequestVerificationToken"]').val();
            if (!token) {
                console.error('Anti-forgery token not found!');
            }

            // Escape HTML function
            function escapeHtml(str) {
                if (typeof str !== 'string') return '';
                return str
                    .replace(/&/g, "&amp;")
                    .replace(/</g, "&lt;")
                    .replace(/>/g, "&gt;")
                    .replace(/"/g, "&quot;")
                    .replace(/'/g, "&#039;");
            }

            // Filter button - AJAX to update table
            $('#filterBtn').click(function (e) {
                e.preventDefault();
                console.log('Filter button clicked');

                var comId = $('#companyDropdown').val();
                var year = parseInt($('#yearDropdown').val());
                var month = parseInt($('#monthDropdown').val());

                console.log('Filter values:', { comId: comId || 'All Companies', year, month });

                if (isNaN(year) || isNaN(month)) {
                    alert('Please select a valid Year and Month.');
                    return;
                }

                // Add timestamp to avoid caching
                let url = comId
                    ? `/Salaries/GetSalariesByCompany?comId=${comId}&year=${year}&month=${month}&_=${new Date().getTime()}`
                    : `/Salaries/GetAllSalaries?year=${year}&month=${month}&_=${new Date().getTime()}`;
                console.log('Fetching salaries from:', url);

                $.getJSON(url, function (data) {
                    console.log('Raw AJAX response:', JSON.stringify(data, null, 2));
                    console.log('Data length:', data.length);
                    $('#salaryTableBody').empty();

                    if (!data || data.length === 0 || (data.success === false)) {
                        console.log('No data or error in response:', data);
                        $('#salaryTableBody').html('<tr><td colspan="12" class="text-center text-muted py-4">No salaries found. Generate some or adjust filters.</td></tr>');
                        return;
                    }

                    $.each(data, function (i, salary) {
                        console.log('Processing salary record:', salary);
                        let row = `
                            <tr id="row-${salary.salaryId}" data-comid="${salary.comId}">
                                <td>${escapeHtml(salary.empName || 'Not Found')}</td>
                                <td>${escapeHtml(salary.salaryMonth)}</td>
                                <td>${salary.gross.toFixed(2)}</td>
                                <td>${salary.basic.toFixed(2)}</td>
                                <td>${salary.hRent.toFixed(2)}</td>
                                <td>${salary.medical.toFixed(2)}</td>
                                <td>${salary.absentDays}</td>
                                <td>${salary.absentAmount.toFixed(2)}</td>
                                <td>${salary.payableAmount.toFixed(2)}</td>
                                <td>${salary.paidAmount.toFixed(2)}</td>
                                <td>${salary.isPaid ? 'Paid' : 'Unpaid'}</td>
                                <td>
                                    <a href="/Salaries/Edit/${salary.salaryId}">Edit</a> |
                                    <button class="payBtn" data-id="${salary.salaryId}">Pay</button> |
                                    <button class="deleteBtn" data-id="${salary.salaryId}">Delete</button>
                                </td>
                            </tr>`;
                        $('#salaryTableBody').append(row);
                        console.log('Appended row:', row);
                    });
                    console.log('Table rendering complete. Rows added:', data.length);
                }).fail(function (xhr, status, error) {
                    console.error('Filter error:', {
                        status: xhr.status,
                        statusText: xhr.statusText,
                        responseText: xhr.responseText,
                        error: error,
                        url: url
                    });
                    $('#salaryTableBody').html(`<tr><td colspan="12" class="text-center text-muted py-4">Error loading salaries: ${xhr.responseJSON?.message || error}</td></tr>`);
                    alert('Failed to filter salaries: ' + (xhr.responseJSON?.message || 'Network error or server unavailable.'));
                });
            });

            // AJAX for Generate Salary
            $('#generateBtn').click(function (e) {
                e.preventDefault();
                console.log('Generate button clicked');

                var comId = $('#companyDropdown').val();
                var year = parseInt($('#yearDropdown').val());
                var month = parseInt($('#monthDropdown').val());

                console.log('Generate values:', { comId, year, month });

                if (!comId || isNaN(year) || isNaN(month)) {
                    alert('Please select a valid Company, Year, and Month.');
                    return;
                }

                var $btn = $(this);
                $btn.prop('disabled', true).text('Generating...');

                $.ajax({
                    url: '/Salaries/Generate',
                    type: 'POST',
                    data: {
                        comId: comId,
                        year: year,
                        month: month
                    },
                    headers: {
                        'RequestVerificationToken': token
                    },
                    success: function (response) {
                        console.log('Generate response:', response);
                        alert(response.message);
                        if (response.success) {
                            setTimeout(() => $('#filterBtn').trigger('click'), 500); // Delay to ensure DB commit
                        }
                    },
                    error: function (xhr, status, error) {
                        console.error('Generate AJAX error:', { status, error, response: xhr.responseText });
                        alert('Error: ' + (xhr.responseJSON?.message || 'Failed to generate salaries.'));
                    },
                    complete: function () {
                        $btn.prop('disabled', false).text('Generate Salary');
                    }
                });
            });

            // AJAX for Pay
            $(document).on('click', '.payBtn', function () {
                var id = $(this).data('id');
                if (!id) return;

                if (!confirm('Mark this salary as paid?')) return;

                $.ajax({
                    url: '/Salaries/MarkAsPaid/' + id,
                    type: 'POST',
                    headers: {
                        'RequestVerificationToken': token
                    },
                    success: function (response) {
                        console.log('Pay response:', response);
                        alert(response.message);
                        if (response.success) {
                            $('#filterBtn').trigger('click'); // Refresh table
                        }
                    },
                    error: function (xhr) {
                        console.error('Pay AJAX error:', xhr.responseText);
                        alert('Error: ' + (xhr.responseJSON?.message || 'Failed to mark as paid.'));
                    }
                });
            });

            // AJAX for Delete
            $(document).on('click', '.deleteBtn', function () {
                var id = $(this).data('id');
                if (!id || !confirm('Are you sure you want to delete?')) return;

                $.ajax({
                    url: '/Salaries/Delete',
                    type: 'POST',
                    data: { id: id },
                    headers: {
                        'RequestVerificationToken': token
                    },
                    success: function (response) {
                        console.log('Delete response:', response);
                        alert(response.message);
                        if (response.success) {
                            $('#filterBtn').trigger('click'); // Refresh table
                        }
                    },
                    error: function (xhr) {
                        console.error('Delete AJAX error:', xhr.responseText);
                        alert('Error: ' + (xhr.responseJSON?.message || 'Failed to delete.'));
                    }
                });
            });
        });
    </script>
}                                                                                              